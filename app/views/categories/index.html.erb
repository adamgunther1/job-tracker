All Categories

<% @categories.each do |category| %>
  <ul><%= link_to category.title, category_path(category) %>  <%= link_to "Edit", edit_category_path(category) %><%= link_to "Delete", category_path(category), method: "delete" %>  </ul>
<% end %>
<br>
<%= link_to "Create New Category", new_category_path %>