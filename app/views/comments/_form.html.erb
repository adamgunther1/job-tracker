<h6>Post a Comment</h6>

<%= form_for [ @job.company, @job, @comment ] do |f| %>
  <p>
    <%= f.label :content %><br/>
    <%= f.text_area :content %>
  </p>
  <p>
    <%= f.submit 'Submit' %>
  </p>
<% end %>